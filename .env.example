# Placeholder for environment variables.
# The Kaggle-only path needs no credentials — keep this file empty.
# If a future iteration adds the Spotify Web API back (grandfathered app
# required, see SPEC.md §1), uncomment and fill these in your local .env:
#
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
#
# Never commit .env — see .gitignore.

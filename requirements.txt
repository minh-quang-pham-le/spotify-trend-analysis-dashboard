# Runtime + dev dependencies, pinned to compatible lower bounds.
# pyproject.toml is the canonical source; this file exists for teammates
# who'd rather use plain pip without an editable install.

# --- Runtime ---
pandas>=2.2
numpy>=1.26
python-dateutil>=2.9

# --- Dev ---
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
